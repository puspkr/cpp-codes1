#include <iostream>
using namespace std;

class Demo {
private:
    int x;
public:
    void setX(int a) { x = a; }
    int getX() { return x; }
};

int main() {
    Demo d;
    d.setX(10);
    cout << d.getX();
}